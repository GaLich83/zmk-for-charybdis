manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
    - name: GaLich83
      url-base: https://github.com/GaLich83
  projects:
    - name: zmk
      remote: zmkfirmware
      revision: v0.3
      import: app/west.yml
    # PMW3610-драйвер: ветка zmk-0.3-motion-flush форка (badjeff-based),
    # запинена на SHA. Совместим с новым pointing-API ZMK v0.3
    # (CONFIG_ZMK_POINTING). Старый DoctorWangWang/main заморожен с 2024
    # и под v0.3 не отдавал движение — отсюда был мёртвый курсор.
    # Эта ветка = zmk-0.3 + motion-flush в конце configure (лечит
    # фантомный рывок курсора вверх-влево на первое касание).
    - name: zmk-pmw3610-driver
      remote: GaLich83
      revision: 6c0af78a9c68da29d0087c2fcfffe662ab6bf312

  self:
    path: config
